// #include "ui/main_window.hpp"
#include <QApplication>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    
    // capvision::ui::MainWindow mainWindow;
    // mainWindow.show();
    
    return app.exec();
}